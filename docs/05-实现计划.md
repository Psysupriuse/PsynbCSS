# 校园活动管理系统 V1.0 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 从零构建校园活动管理系统 V1.0 Web 网站：学生注册登录、浏览报名活动；教师发布管理活动；角色权限隔离；Git 9 步迭代提交。

**Architecture:** Express 4 服务端渲染（EJS），express-session 会话鉴权，node:sqlite 内置数据库。学生端顶部导航、教师端侧边菜单两套视图，蓝色主题。权限由 requireLogin/requireStudent/requireTeacher 中间件链保证。

**Tech Stack:** Node.js v24（node:sqlite、crypto.scrypt）、Express ^4.21、EJS ^3.1、express-session ^1.18

**Spec:** 需求 `docs/01-开发需求文档.md`；技术设计 `docs/02-技术设计文档.md`；UI/代码规范 `docs/03-设计规范.md`；执行步骤 `docs/04-开发执行步骤.md`

## Global Constraints

- 依赖仅 3 个纯 JS 包：`express@^4.21`、`ejs@^3.1`、`express-session@^1.18`；其余一律用 Node 内置模块
- 配色（CSS 变量）：主色 `#1677ff`、浅蓝 `#e8f3ff`、文字 `#222`/`#666`、成功 `#00b42a`、警告 `#ff7d00`、危险 `#f53f3f`、背景 `#f7f9fc`
- 活动时间格式统一 `YYYY-MM-DD HH:MM`（本地时间）；`活动时间 <= 当前时间` 即"已结束"；状态不落库、查询时计算
- 状态判定优先级：已结束 > 已满员 > 未开始；badge 颜色：未开始=绿、已满员=红、已结束=橙
- 密码 scrypt 加盐哈希存储（格式 `salt:hash`，hex），任何页面不回显密码；登录失败统一提示"用户名或密码错误"
- SQL 一律 `db.prepare` 参数化；JS 2 空格缩进、分号、单引号、中文注释
- 页面文字中文；路由命名按 docs/02 第 5 节；类名 kebab-case
- `.gitignore` 排除 `node_modules/`、`data/`、`.env`、`*.log`；`data/app.db` 运行时自动生成
- 每步自测通过后才 commit，提交信息格式：`步骤N: 做了什么`；每步尝试 `git push origin main`（本机直连 GitHub 不通时保留提交、devlog 记录"待补推"）
- 服务端口 3000（`PORT` 环境变量可覆盖）；会话 MemoryStore（重启需重新登录，V1.0 可接受）
- V1.0 不实现：审批、签到、评价、评论、图片上传、Excel 导出、管理员角色、短信邮件通知

## 文件结构总览

```
app.js                  # 入口：会话/静态资源/flash 注入/路由挂载/错误处理/启动
db.js                   # node:sqlite 初始化、建表、演示种子数据；exports: initDb(), getDb()
auth.js                 # 注册/登录/登出路由 + changePassword 处理器；exports: Router（含 .changePassword 属性）
middleware.js           # exports: requireLogin, requireStudent, requireTeacher, setFlash(req,type,text)
utils/password.js       # exports: hashPassword(pw), verifyPassword(pw, stored)
utils/activity-status.js# exports: getActivityStatus(timeStr, count, max), formatLocalTime(date)
routes/student.js       # 学生端路由；exports: Router
routes/teacher.js       # 教师端路由；exports: Router
views/partials/head.ejs         # <head> + css 引入 + <title>
views/partials/flash.ejs        # flash 提示条（读取 res.locals.flash）
views/partials/student-nav.ejs  # 学生顶部蓝导航（读 res.locals.currentUser）
views/partials/teacher-sidebar.ejs # 教师左侧菜单（参数 active）
views/register.ejs / login.ejs / 403.ejs / 404.ejs / 500.ejs
views/student/index.ejs / detail.ejs / my.ejs / profile.ejs
views/teacher/dashboard.ejs / activities.ejs / activity-form.ejs / roster.ejs / profile.ejs
public/css/style.css    # 唯一全局样式（docs/03 配色）
public/js/main.js       # 删除确认等少量交互
test/smoke.js           # 冒烟验证脚本（Task 9）
data/app.db             # 运行时生成（gitignore）
```

**会话数据**：`req.session.user = { id, username, role }`（role ∈ 'student' | 'teacher'）
**Flash 数据**：`req.session.flash = { type: 'success'|'error', text: string }`，每次请求注入 `res.locals.flash` 后清除

---

### Task 1: 项目初始化（git init、骨架、README、远程配置）

**Files:**
- Create: `.gitignore`、`package.json`、`README.md`、`app.js`（占位可启动）
- Create 空目录：`routes/`、`views/`、`public/css/`、`public/js/`、`test/`、`data/`、`utils/`

**Interfaces:**
- Produces: 可启动的 Express 空壳；npm scripts：`npm start`、`npm run smoke`（后者 Task 9 前报"文件不存在"属正常）

- [ ] **Step 1: git init 与 .gitignore**

```bash
cd "D:\校园活动管理系统"
git init -b main
git remote add origin https://github.com/Psysupriuse/PsynbCSS.git
```

创建 `.gitignore`：

```gitignore
node_modules/
data/
.env
*.log
```

- [ ] **Step 2: package.json 并安装依赖**

创建 `package.json`：

```json
{
  "name": "campus-activity-system",
  "version": "1.0.0",
  "private": true,
  "description": "校园活动管理系统 V1.0 —— 软件工程迭代开发实验",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "smoke": "node test/smoke.js"
  }
}
```

运行：`npm install express@^4.21 ejs@^3.1 express-session@^1.18`

- [ ] **Step 3: 占位 app.js（可启动）**

```js
// 校园活动管理系统 V1.0 入口（占位，后续任务逐步完善）
const express = require('express');

const app = express();
app.set('view engine', 'ejs');

// 占位首页
app.get('/', (req, res) => res.send('<h1>校园活动管理系统 V1.0 开发中</h1>'));

const PORT = process.env.PORT || 3000;
app.listen(PORT, () => console.log(`校园活动管理系统已启动: http://localhost:${PORT}`));
```

- [ ] **Step 4: README.md**

内容：项目名称与一句话简介；课程实验背景；技术栈；目录结构；`npm install` / `npm start` 启动方式；9 步迭代计划（指向 docs/04）；"AI 生成代码已经人工校验"声明（随进度更新）。

- [ ] **Step 5: 自测**

```bash
npm start          # 应输出 "校园活动管理系统已启动: http://localhost:3000"
# 另开终端：
curl -s http://localhost:3000/ | grep "开发中"
```
预期：grep 命中占位标题。测完 Ctrl+C 停止。

- [ ] **Step 6: 提交**

```bash
git add .
git commit -m "步骤1: 初始化项目结构、README、需求与技术设计文档"
git push origin main
```

若 push 报 "Failed to connect"：保留提交，在 `devlog/2026-09-20.md` 待办中标注"待补推"。

---

### Task 2: 注册登录页面基础 UI（蓝色主题）

**Files:**
- Create: `public/css/style.css`、`views/partials/head.ejs`、`views/partials/flash.ejs`、`views/login.ejs`、`views/register.ejs`
- Modify: `app.js`（挂 GET /login、/register 渲染视图）

**Interfaces:**
- Produces: CSS 类名（后续任务复用）：`.container`、`.auth-card`、`.btn .btn-primary .btn-secondary .btn-danger`、`.flash .flash-success .flash-error`、`.badge .badge-success .badge-warning .badge-danger`、`.form-group .form-input .form-label`、`.top-nav`、`.side-layout .side-menu .side-main`、`.card .activity-card`、`.table`、`.empty-state`、`.stat-cards .stat-card`
- head.ejs 用法：`<%- include('partials/head', { title: '登录' }) %>`；flash.ejs 用法：`<%- include('partials/flash') %>`

- [ ] **Step 1: style.css —— 按 docs/03 配色与组件规范写全局样式**

关键骨架（其余选择器按 docs/03 第 3 节逐条实现，颜色一律引用变量）：

```css
/* 主题变量（docs/03 配色） */
:root {
  --primary: #1677ff;
  --primary-light: #e8f3ff;
  --text-main: #222222;
  --text-sub: #666666;
  --success: #00b42a;
  --warning: #ff7d00;
  --danger: #f53f3f;
  --bg: #f7f9fc;
  --radius: 8px;
}

body { margin: 0; font-family: -apple-system, 'Microsoft YaHei', sans-serif;
       background: var(--bg); color: var(--text-main); }

/* 登录/注册卡片 */
.auth-card { background: #fff; border-radius: var(--radius); width: 400px;
             margin: 10vh auto; padding: 32px; box-shadow: 0 2px 8px rgba(0,0,0,.06); }

/* 按钮三态 */
.btn-primary { background: var(--primary); color: #fff; }
.btn-secondary { background: #fff; color: var(--primary); border: 1px solid var(--primary); }
.btn-danger { background: var(--danger); color: #fff; }

/* badge / flash 颜色按 docs/03 3.5、3.6 */
```

- [ ] **Step 2: head.ejs 与 flash.ejs 公共片段**

`views/partials/head.ejs`：

```ejs
<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %> - 校园活动管理系统</title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
```

`views/partials/flash.ejs`：

```ejs
<% if (flash) { %>
  <div class="flash flash-<%= flash.type %>"><%= flash.text %></div>
<% } %>
```

- [ ] **Step 3: login.ejs / register.ejs（docs/01 4.1 的内容，表单已指向真实 action）**

`views/login.ejs`：

```ejs
<%- include('partials/head', { title: '登录' }) %>
<%- include('partials/flash') %>
<div class="auth-card">
  <h1 class="auth-title">校园活动管理系统</h1>
  <form action="/login" method="POST">
    <div class="form-group">
      <label class="form-label" for="username">用户名 <span class="required">*</span></label>
      <input class="form-input" type="text" id="username" name="username" required>
    </div>
    <div class="form-group">
      <label class="form-label" for="password">密码 <span class="required">*</span></label>
      <input class="form-input" type="password" id="password" name="password" required>
    </div>
    <button class="btn btn-primary btn-block" type="submit">登录</button>
  </form>
  <p class="auth-switch">还没有账号？<a href="/register">立即注册</a></p>
</div>
</body>
</html>
```

`views/register.ejs` 同结构，表单字段：`username`（text）、`password`（password）、`confirm`（password，确认密码）、`role`（select，选项"学生 student / 教师 teacher"，默认学生）；按钮"注册"；底部链接"已有账号？去登录 → /login"。action="/register" method="POST"。

- [ ] **Step 4: app.js 挂 GET 路由**

```js
app.set('view engine', 'ejs');
app.set('views', path.join(__dirname, 'views'));
app.get('/login', (req, res) => res.render('login'));
app.get('/register', (req, res) => res.render('register'));
```

- [ ] **Step 5: 自测**

浏览器打开 `http://localhost:3000/login` 和 `/register`：蓝色主题、卡片居中、表单字段齐全；样式无错乱。两个页面互跳链接可用。（此时提交表单会 404，属预期，Task 3 解决）

- [ ] **Step 6: 提交**

```bash
git add .
git commit -m "步骤2: 完成注册登录页面基础UI，蓝色主题样式"
git push origin main   # 不通则 devlog 记录待补推
```

---

### Task 3: 注册登录业务逻辑，区分角色

**Files:**
- Create: `db.js`（users 表）、`utils/password.js`、`middleware.js`、`auth.js`、`views/403.ejs`、`views/404.ejs`、`views/500.ejs`
- Modify: `app.js`（会话、静态资源、body 解析、flash 注入、路由挂载、404/500 处理）

**Interfaces:**
- Consumes: `hashPassword`/`verifyPassword`（本任务定义，db.js 种子数据在 Task 4 使用）
- Produces: `initDb()`、`getDb()` → DatabaseSync；`requireLogin/requireStudent/requireTeacher`；`setFlash(req, type, text)`；`auth` Router 带属性 `auth.changePassword(req, res)`（Task 5 后的个人中心复用）；`res.locals.flash`、`res.locals.currentUser`

- [ ] **Step 1: utils/password.js**

```js
// scrypt 加盐密码哈希（Node 内置，无额外依赖）
const crypto = require('node:crypto');

// 生成 'salt:hash' 格式的密码哈希
function hashPassword(password) {
  const salt = crypto.randomBytes(16).toString('hex');
  const hash = crypto.scryptSync(password, salt, 64).toString('hex');
  return `${salt}:${hash}`;
}

// 校验密码；格式不对或密码不符均返回 false
function verifyPassword(password, stored) {
  const [salt, hash] = String(stored || '').split(':');
  if (!salt || !hash) return false;
  const candidate = crypto.scryptSync(password, salt, 64).toString('hex');
  return crypto.timingSafeEqual(Buffer.from(candidate, 'hex'), Buffer.from(hash, 'hex'));
}

module.exports = { hashPassword, verifyPassword };
```

- [ ] **Step 2: db.js（先只建 users 表）**

```js
// SQLite 初始化与连接（Node 24 内置 node:sqlite）
const { DatabaseSync } = require('node:sqlite');
const path = require('node:path');
const fs = require('node:fs');

const DATA_DIR = path.join(__dirname, 'data');
const DB_PATH = path.join(DATA_DIR, 'app.db');

let db;

function initDb() {
  if (!fs.existsSync(DATA_DIR)) fs.mkdirSync(DATA_DIR, { recursive: true });
  db = new DatabaseSync(DB_PATH);
  db.exec(`
    CREATE TABLE IF NOT EXISTS users (
      id            INTEGER PRIMARY KEY AUTOINCREMENT,
      username      TEXT NOT NULL UNIQUE,
      password_hash TEXT NOT NULL,
      role          TEXT NOT NULL CHECK (role IN ('student','teacher')),
      created_at    TEXT NOT NULL DEFAULT (datetime('now','localtime'))
    );
  `);
  return db;
}

function getDb() {
  return db;
}

module.exports = { initDb, getDb };
```

- [ ] **Step 3: middleware.js**

```js
// 登录与角色校验中间件
function requireLogin(req, res, next) {
  if (req.session.user) return next();
  return res.redirect('/login');
}

function requireRole(role) {
  return (req, res, next) => {
    if (req.session.user && req.session.user.role === role) return next();
    return res.status(403).render('403', { message: '没有权限访问该页面' });
  };
}

// 一次性操作提示：写入会话，下一次请求的视图读取后清除
function setFlash(req, type, text) {
  req.session.flash = { type, text };
}

module.exports = {
  requireLogin,
  requireStudent: requireRole('student'),
  requireTeacher: requireRole('teacher'),
  setFlash,
};
```

- [ ] **Step 4: auth.js（注册/登录/登出 + 改密码处理器）**

```js
// 账号相关路由：注册、登录、登出
const express = require('express');
const { getDb } = require('./db');
const { hashPassword, verifyPassword } = require('./utils/password');
const { setFlash } = require('./middleware');

const router = express.Router();

router.get('/register', (req, res) => res.render('register'));
router.get('/login', (req, res) => res.render('login'));

// FR-01 注册：用户名唯一；两次密码一致；角色合法
router.post('/register', (req, res) => {
  const username = String(req.body.username || '').trim();
  const password = String(req.body.password || '');
  const confirm = String(req.body.confirm || '');
  const role = String(req.body.role || '');

  if (!username || !password || !confirm) {
    setFlash(req, 'error', '请填写完整的注册信息');
    return res.redirect('/register');
  }
  if (password !== confirm) {
    setFlash(req, 'error', '两次输入的密码不一致');
    return res.redirect('/register');
  }
  if (!['student', 'teacher'].includes(role)) {
    setFlash(req, 'error', '请选择有效的用户角色');
    return res.redirect('/register');
  }
  const exists = getDb().prepare('SELECT id FROM users WHERE username = ?').get(username);
  if (exists) {
    setFlash(req, 'error', '用户名已存在，请更换');
    return res.redirect('/register');
  }
  getDb().prepare('INSERT INTO users (username, password_hash, role) VALUES (?, ?, ?)')
    .run(username, hashPassword(password), role);
  setFlash(req, 'success', '注册成功，请登录');
  res.redirect('/login');
});

// FR-02 登录：校验通过写会话，按角色跳转（FR 角色区分）
router.post('/login', (req, res) => {
  const username = String(req.body.username || '').trim();
  const password = String(req.body.password || '');
  const user = getDb().prepare('SELECT * FROM users WHERE username = ?').get(username);
  if (!user || !verifyPassword(password, user.password_hash)) {
    setFlash(req, 'error', '用户名或密码错误');
    return res.redirect('/login');
  }
  req.session.user = { id: user.id, username: user.username, role: user.role };
  res.redirect(user.role === 'teacher' ? '/teacher' : '/student');
});

// FR-04 退出登录
router.post('/logout', (req, res) => {
  req.session.destroy(() => res.redirect('/login'));
});

// FR-03 修改密码处理器（学生/教师个人中心 POST 复用）
function changePassword(req, res) {
  const current = String(req.body.current || '');
  const next = String(req.body.password || '');
  const confirm = String(req.body.confirm || '');
  const user = getDb().prepare('SELECT * FROM users WHERE id = ?').get(req.session.user.id);

  if (!verifyPassword(current, user.password_hash)) {
    setFlash(req, 'error', '当前密码不正确');
    return res.redirect('back');
  }
  if (!next || next !== confirm) {
    setFlash(req, 'error', '两次输入的新密码不一致');
    return res.redirect('back');
  }
  getDb().prepare('UPDATE users SET password_hash = ? WHERE id = ?')
    .run(hashPassword(next), user.id);
  setFlash(req, 'success', '密码修改成功');
  res.redirect('back');
}
router.changePassword = changePassword;

module.exports = router;
```

- [ ] **Step 5: 403/404/500 错误页**

三个视图结构相同（蓝主题居中）：状态码大号显示、message 文案（403 页显示传入的 `message`，默认"没有权限访问该页面"；404 页"页面不存在"；500 页"服务器内部错误"）+ 返回链接（`<a href="/">返回首页</a>`）。500 页不显示错误堆栈。

- [ ] **Step 6: app.js 完整装配**

```js
// 校园活动管理系统 V1.0 入口
const express = require('express');
const session = require('express-session');
const path = require('node:path');
const crypto = require('node:crypto');
const { initDb } = require('./db');
const authRouter = require('./auth');
const { requireLogin, requireStudent, requireTeacher } = require('./middleware');

initDb();

const app = express();
app.set('view engine', 'ejs');
app.set('views', path.join(__dirname, 'views'));

app.use(express.urlencoded({ extended: false }));
app.use(express.static(path.join(__dirname, 'public')));
// 会话：密钥优先取环境变量，否则随机生成（重启后需重新登录）
app.use(session({
  secret: process.env.SESSION_SECRET || crypto.randomBytes(32).toString('hex'),
  resave: false,
  saveUninitialized: false,
  cookie: { httpOnly: true },
}));

// 注入一次性提示与当前登录用户到所有视图
app.use((req, res, next) => {
  res.locals.flash = req.session.flash || null;
  if (req.session.flash) delete req.session.flash;
  res.locals.currentUser = req.session.user || null;
  next();
});

app.use('/', authRouter);

// 根路径按登录状态与角色跳转
app.get('/', (req, res) => {
  if (!req.session.user) return res.redirect('/login');
  return res.redirect(req.session.user.role === 'teacher' ? '/teacher' : '/student');
});

// 404 与统一错误处理
app.use((req, res) => res.status(404).render('404'));
app.use((err, req, res, next) => {
  console.error(err);
  res.status(500).render('500');
});

const PORT = process.env.PORT || 3000;
app.listen(PORT, () => console.log(`校园活动管理系统已启动: http://localhost:${PORT}`));
```

注意：`/student`、`/teacher` 挂载在 Task 4/5 完成，本任务结束时 `/student` 仍 404——**自测步骤用"登录后按角色跳转"验证跳转行为**（跳转后 404 页面属预期，devlog 记录）。为保证跳转验证可行，本任务先创建 `routes/student.js`、`routes/teacher.js` 两个**占位 Router**（仅 `module.exports = express.Router()`）并在 app.js 挂载：

```js
const studentRouter = require('./routes/student');
const teacherRouter = require('./routes/teacher');
app.use('/student', requireLogin, requireStudent, studentRouter);
app.use('/teacher', requireLogin, requireTeacher, teacherRouter);
```

- [ ] **Step 7: 自测（curl 全链路）**

```bash
npm start
# 1) 注册学生
curl -s -c /tmp/jar.txt -X POST http://localhost:3000/register \
  -d "username=stu_test&password=123456&confirm=123456&role=student" -o /dev/null -w "%{http_code}\n"
# 预期 302
# 2) 重复注册被拒（跟随重定向检查提示）
curl -s -L -X POST http://localhost:3000/register \
  -d "username=stu_test&password=123456&confirm=123456&role=student" | grep "用户名已存在"
# 3) 密码不一致被拒：... confirm=654321 ... | grep "两次输入的密码不一致"
# 4) 登录错误密码：curl -s -L -X POST http://localhost:3000/login -d "username=stu_test&password=wrong" | grep "用户名或密码错误"
# 5) 登录成功跳转学生端（携带 cookie）：
curl -s -c /tmp/jar.txt -X POST http://localhost:3000/login -d "username=stu_test&password=123456" -o /dev/null -w "%{http_code} %{redirect_url}\n"
# 预期 "302 http://localhost:3000/student"
curl -s -b /tmp/jar.txt http://localhost:3000/student -o /dev/null -w "%{http_code}\n"   # 预期 404（占位 Router 无路由，Task 4 补页面）
# 6) 未登录访问 /student 被重定向：
curl -s http://localhost:3000/student -o /dev/null -w "%{http_code} %{redirect_url}\n"   # 预期 302 /login
```

浏览器人工校验：注册页各错误提示、登录成功跳转、注册教师账号 `tea_test`（role=teacher）登录跳转 `/teacher`。登录后手动访问 `/logout` 无效（POST 才生效），后续页面有退出按钮。

- [ ] **Step 8: 提交**

```bash
git add .
git commit -m "步骤3: 实现注册登录业务逻辑，区分角色权限跳转"
git push origin main
```

---

### Task 4: 学生端活动列表、详情页（含种子数据）

**Files:**
- Create: `utils/activity-status.js`、`views/partials/student-nav.ejs`、`views/student/index.ejs`、`views/student/detail.ejs`、`views/student/profile.ejs`
- Modify: `db.js`（activities 表 + 演示种子数据）、`routes/student.js`（列表/详情/个人中心路由）

**Interfaces:**
- Produces: `getActivityStatus(timeStr, currentCount, max)` → `{ key: 'upcoming'|'full'|'ended', label: '未开始'|'已满员'|'已结束', color: 'success'|'danger'|'warning' }`（已结束优先于已满员）；`formatLocalTime(date)` → `'YYYY-MM-DD HH:MM'`（Task 6 统计用）
- 视图数据：列表页 `{ activities }`，每项含 `a.* + teacher_name + registered_count + status`；详情页 `{ activity }` 另含 `registered_by_me`（0/1，Task 7 按钮状态用）

- [ ] **Step 1: utils/activity-status.js**

```js
// 活动状态判定与时间格式化（状态不落库，查询时计算）
function formatLocalTime(date) {
  const p = (n) => String(n).padStart(2, '0');
  return `${date.getFullYear()}-${p(date.getMonth() + 1)}-${p(date.getDate())} ${p(date.getHours())}:${p(date.getMinutes())}`;
}

// 判定优先级：已结束 > 已满员 > 未开始（docs/02 第 4 节、docs/03 3.5）
function getActivityStatus(timeStr, currentCount, maxParticipants) {
  if (new Date(timeStr) <= new Date()) {
    return { key: 'ended', label: '已结束', color: 'warning' };
  }
  if (currentCount >= maxParticipants) {
    return { key: 'full', label: '已满员', color: 'danger' };
  }
  return { key: 'upcoming', label: '未开始', color: 'success' };
}

module.exports = { formatLocalTime, getActivityStatus };
```

- [ ] **Step 2: db.js 增加 activities 表与种子数据**

```js
// 在 users 建表语句后追加：
db.exec(`
  CREATE TABLE IF NOT EXISTS activities (
    id               INTEGER PRIMARY KEY AUTOINCREMENT,
    title            TEXT NOT NULL,
    time             TEXT NOT NULL,
    location         TEXT NOT NULL,
    description      TEXT NOT NULL,
    max_participants INTEGER NOT NULL,
    teacher_id       INTEGER NOT NULL REFERENCES users(id),
    created_at       TEXT NOT NULL DEFAULT (datetime('now','localtime'))
  );
`);
seedIfEmpty();
```

种子函数（演示数据，README 记录演示账号）：

```js
const { hashPassword } = require('./utils/password');

// 首次建库时写入演示教师与示例活动，便于学生端页面展示
function seedIfEmpty() {
  const count = db.prepare('SELECT COUNT(*) AS c FROM activities').get().c;
  if (count > 0) return;
  db.prepare('INSERT INTO users (username, password_hash, role) VALUES (?, ?, ?)')
    .run('teacher_demo', hashPassword('demo123456'), 'teacher');
  const teacher = db.prepare("SELECT id FROM users WHERE username = 'teacher_demo'").get();
  const sample = [
    ['校园迎新晚会', '2026-10-15 19:00', '大学生活动中心', '欢迎新同学，节目表演与互动环节。', 200],
    ['篮球友谊赛', '2026-09-25 16:00', '东区篮球场', '计算机学院 vs 机械学院，欢迎观赛。', 50],
    ['图书馆读书分享会', '2026-09-10 14:00', '图书馆报告厅', '分享一本好书，交流阅读心得。', 80],
  ];
  const insert = db.prepare('INSERT INTO activities (title, time, location, description, max_participants, teacher_id) VALUES (?, ?, ?, ?, ?, ?)');
  for (const row of sample) insert.run(...row, teacher.id);
}
```

种子时间说明：`2026-09-10` 那条已过期（演示"已结束"badge），其余为未开始——覆盖两种状态展示。

- [ ] **Step 3: routes/student.js（列表 + 详情）**

```js
// 学生端：活动浏览
const express = require('express');
const { getDb } = require('../db');
const { getActivityStatus } = require('../utils/activity-status');

const router = express.Router();

// FR-05 活动列表（可按标题/地点关键字筛选）
router.get('/', (req, res) => {
  const keyword = String(req.query.q || '').trim();
  const db = getDb();
  let sql = `
    SELECT a.*, u.username AS teacher_name,
      (SELECT COUNT(*) FROM registrations r WHERE r.activity_id = a.id) AS registered_count
    FROM activities a
    JOIN users u ON u.id = a.teacher_id`;
  const params = [];
  if (keyword) {
    sql += ' WHERE a.title LIKE ? OR a.location LIKE ?';
    params.push(`%${keyword}%`, `%${keyword}%`);
  }
  sql += ' ORDER BY a.time DESC';
  const activities = db.prepare(sql).all(...params);
  for (const a of activities) {
    a.status = getActivityStatus(a.time, a.registered_count, a.max_participants);
  }
  res.render('student/index', { activities, keyword });
});

// FR-06 活动详情
router.get('/activities/:id', (req, res) => {
  const db = getDb();
  const activity = db.prepare(`
    SELECT a.*, u.username AS teacher_name,
      (SELECT COUNT(*) FROM registrations r WHERE r.activity_id = a.id) AS registered_count,
      EXISTS(SELECT 1 FROM registrations r WHERE r.activity_id = a.id AND r.student_id = ?) AS registered_by_me
    FROM activities a
    JOIN users u ON u.id = a.teacher_id
    WHERE a.id = ?`).get(req.session.user.id, Number(req.params.id));
  if (!activity) return res.status(404).render('404');
  activity.status = getActivityStatus(activity.time, activity.registered_count, activity.max_participants);
  res.render('student/detail', { activity });
});

module.exports = router;
```

注意：本任务 registrations 表尚不存在（Task 7 建），上述 SQL 会报错。**临时处理**：本任务先把 `registered_count` 用 `0`、`registered_by_me` 用 `0` 代替（写死注释标记），Task 7 换回子查询。即在 Task 4 的 SQL 中删去两个子查询列，JS 中赋 `a.registered_count = 0`。

- [ ] **Step 4: student-nav.ejs（顶部蓝色导航）**

```ejs
<nav class="top-nav">
  <a class="brand" href="/student">校园活动管理系统</a>
  <div class="nav-links">
    <a href="/student" class="<%= active === 'index' ? 'active' : '' %>">首页</a>
    <a href="/student/my" class="<%= active === 'my' ? 'active' : '' %>">我的报名</a>
    <a href="/student/profile" class="<%= active === 'profile' ? 'active' : '' %>">个人中心</a>
    <form action="/logout" method="POST">
      <button type="submit" class="nav-logout">退出登录（<%= currentUser.username %>）</button>
    </form>
  </div>
</nav>
```

CSS 补充：`.top-nav`（主色底白字）、`.nav-links a.active`（半透明白底高亮）、`.nav-logout`（无边框透明按钮白字）。

- [ ] **Step 5: student/index.ejs（卡片列表 + 筛选框）**

```ejs
<%- include('partials/head', { title: '活动列表' }) %>
<%- include('partials/student-nav', { active: 'index' }) %>
<main class="container">
  <%- include('partials/flash') %>
  <div class="page-head">
    <h2>校园活动</h2>
    <form action="/student" method="GET" class="search-form">
      <input class="form-input" type="text" name="q" value="<%= keyword %>" placeholder="按活动标题或地点搜索">
      <button class="btn btn-primary" type="submit">搜索</button>
    </form>
  </div>
  <% if (activities.length === 0) { %>
    <div class="empty-state">暂无活动</div>
  <% } %>
  <div class="card-grid">
    <% activities.forEach(a => { %>
      <div class="card activity-card">
        <div class="card-head">
          <h3 class="card-title"><%= a.title %></h3>
          <span class="badge badge-<%= a.status.color %>"><%= a.status.label %></span>
        </div>
        <div class="card-body">
          <p class="meta">时间：<%= a.time %></p>
          <p class="meta">地点：<%= a.location %></p>
          <p class="meta">主办方：<%= a.teacher_name %></p>
          <p class="card-desc"><%= a.description %></p>
        </div>
        <div class="card-foot">
          <a class="btn btn-primary" href="/student/activities/<%= a.id %>">查看详情</a>
        </div>
      </div>
    <% }) %>
  </div>
</main>
</body>
</html>
```

- [ ] **Step 6: student/detail.ejs（详情 + 报名按钮占位）**

内容：`<%- include('partials/flash') %>`（报名等操作提示显示在此页）；标题 + 状态 badge；信息表（时间/地点/主办方 `teacher_name`/最大报名人数 `max_participants`/已报名 `registered_count`）；活动介绍全文；按钮区（Task 7 前为占位）：`<button class="btn btn-primary" disabled>立即报名（开发中）</button>`。返回链接 `/student`。

- [ ] **Step 7: 个人中心 profile.ejs + 路由（FR-03 修改密码、FR-04 退出登录入口）**

`views/student/profile.ejs`（顶部导航 active: 'profile'）：账号信息卡（用户名 `currentUser.username`、角色"学生"）；修改密码表单：

```ejs
<form action="/student/profile" method="POST" class="card form-card">
  <div class="form-group">
    <label class="form-label" for="current">当前密码 <span class="required">*</span></label>
    <input class="form-input" type="password" id="current" name="current" required>
  </div>
  <div class="form-group">
    <label class="form-label" for="password">新密码 <span class="required">*</span></label>
    <input class="form-input" type="password" id="password" name="password" required>
  </div>
  <div class="form-group">
    <label class="form-label" for="confirm">确认新密码 <span class="required">*</span></label>
    <input class="form-input" type="password" id="confirm" name="confirm" required>
  </div>
  <button class="btn btn-primary" type="submit">修改密码</button>
</form>
```

`routes/student.js` 追加（复用 Task 3 的 changePassword 处理器）：

```js
const auth = require('../auth');

// 个人中心
router.get('/profile', (req, res) => res.render('student/profile'));
router.post('/profile', auth.changePassword);
```

- [ ] **Step 8: 自测**

```bash
# 重启服务（db.js 变更需重启）：先停旧进程再 npm start
# 会话密钥随机生成，重启后旧 cookie 失效，需重新登录：
curl -s -c /tmp/jar.txt -X POST http://localhost:3000/login -d "username=stu_test&password=123456" -o /dev/null -w "%{http_code} %{redirect_url}\n"
curl -s -b /tmp/jar.txt http://localhost:3000/student | grep -c "activity-card"   # 预期 3（3 条种子活动）
curl -s -b /tmp/jar.txt "http://localhost:3000/student?q=篮球" | grep "篮球友谊赛"
curl -s -b /tmp/jar.txt http://localhost:3000/student/activities/1 -o /dev/null -w "%{http_code}\n"  # 预期 200
curl -s -b /tmp/jar.txt http://localhost:3000/student/activities/999 -o /dev/null -w "%{http_code}\n"  # 预期 404
```

浏览器校验：卡片蓝色主题、时间倒序、`2026-09-10` 读书分享会显示橙色"已结束"、其余绿色"未开始"；筛选可用；详情页信息完整；个人中心显示账号信息，修改密码成功后有提示、用新密码可重新登录。

**种子数据说明**：种子在 activities 表为空时自动写入（首次启动即有 3 条示例活动），无需手动删库；demo 教师账号 `teacher_demo / demo123456`（README 记录）。

- [ ] **Step 9: 提交**

```bash
git add .
git commit -m "步骤4: 学生端活动列表与详情页，演示种子数据"
git push origin main
```

---

### Task 5: 教师端后台框架（侧边菜单、统计占位、活动表单页）

**Files:**
- Create: `views/partials/teacher-sidebar.ejs`、`views/teacher/dashboard.ejs`、`views/teacher/activities.ejs`、`views/teacher/activity-form.ejs`、`views/teacher/profile.ejs`
- Modify: `routes/teacher.js`（GET 路由渲染页面，统计先传 0）

**Interfaces:**
- 视图数据：dashboard `{ total, upcoming, ended }`；activities `{ activities }`（本任务传空数组）；activity-form `{ activity: { title, time, location, description, max_participants }, mode: 'new'|'edit', errors 可选 }`

- [ ] **Step 1: teacher-sidebar.ejs（左侧菜单布局）**

```ejs
<div class="side-layout">
  <aside class="side-menu">
    <div class="side-brand">校园活动管理系统</div>
    <a href="/teacher" class="<%= active === 'dashboard' ? 'active' : '' %>">后台首页</a>
    <a href="/teacher/activities" class="<%= active === 'activities' ? 'active' : '' %>">活动管理</a>
    <a href="/teacher/profile" class="<%= active === 'profile' ? 'active' : '' %>">个人中心</a>
    <form action="/logout" method="POST">
      <button type="submit" class="side-logout">退出登录（<%= currentUser.username %>）</button>
    </form>
  </aside>
  <main class="side-main">
    <%- include('partials/flash') %>
```

侧边栏是包裹布局的开始标签（每页手动闭合 `</main></div>`），无模板布局引擎，各页面结构：

```ejs
<%- include('partials/head', { title: '后台首页' }) %>
<%- include('partials/teacher-sidebar', { active: 'dashboard' }) %>
    ...页面内容...
  </main>
</div>
</body>
</html>
```

CSS：`.side-layout`（flex，全高）、`.side-menu`（约 220px 白底，主色选中态）、`.side-main`（剩余宽度，内边距）。

- [ ] **Step 2: dashboard.ejs（统计占位）**

三张统计卡 `.stat-cards`：总活动数 / 待开始 / 已结束，数值用 `<%= total %>` 等（本任务路由传 0）；下方说明文字"统计数据将在活动管理功能完成后生效"。复用卡片样式：`.stat-card`（白底圆角，数字大号主色）。

- [ ] **Step 3: activities.ejs（活动管理列表空态）**

结构：标题"活动管理" + 右侧"新增活动"按钮（`/teacher/activities/new`）；表格 `.table`（列：标题/时间/状态/操作）；本任务数据为空 → 显示 `.empty-state`"暂无活动，点击右上角新增"。

- [ ] **Step 4: activity-form.ejs（新增/编辑共用表单）**

```ejs
<form action="<%= mode === 'edit' ? '/teacher/activities/' + activity.id + '/edit' : '/teacher/activities/new' %>" method="POST" class="card form-card">
  <div class="form-group">
    <label class="form-label" for="title">活动标题 <span class="required">*</span></label>
    <input class="form-input" type="text" id="title" name="title" value="<%= activity.title || '' %>" required>
  </div>
  <div class="form-group">
    <label class="form-label" for="time">活动时间 <span class="required">*</span></label>
    <input class="form-input" type="text" id="time" name="time" value="<%= activity.time || '' %>" placeholder="格式：YYYY-MM-DD HH:MM，如 2026-10-15 19:00" required>
  </div>
  <div class="form-group">
    <label class="form-label" for="location">活动地点 <span class="required">*</span></label>
    <input class="form-input" type="text" id="location" name="location" value="<%= activity.location || '' %>" required>
  </div>
  <div class="form-group">
    <label class="form-label" for="description">活动简介 <span class="required">*</span></label>
    <textarea class="form-input" id="description" name="description" rows="4" required><%= activity.description || '' %></textarea>
  </div>
  <div class="form-group">
    <label class="form-label" for="max_participants">最大报名人数 <span class="required">*</span></label>
    <input class="form-input" type="number" id="max_participants" name="max_participants" value="<%= activity.max_participants || '' %>" min="1" required>
  </div>
  <div class="form-actions">
    <button class="btn btn-primary" type="submit">保存</button>
    <a class="btn btn-secondary" href="/teacher/activities">取消</a>
  </div>
</form>
```

字段即 docs/01 4.3.3 要求的五项：标题、时间、地点、简介、最大报名人数。

- [ ] **Step 5: routes/teacher.js（GET 路由）**

```js
// 教师端：活动管理（本任务仅页面框架，统计与列表 Task 6 接数据）
const express = require('express');

const router = express.Router();

router.get('/', (req, res) => {
  res.render('teacher/dashboard', { total: 0, upcoming: 0, ended: 0 });
});

router.get('/activities', (req, res) => {
  res.render('teacher/activities', { activities: [] });
});

router.get('/activities/new', (req, res) => {
  res.render('teacher/activity-form', {
    mode: 'new',
    activity: { title: '', time: '', location: '', description: '', max_participants: '' },
  });
});

module.exports = router;
```

- [ ] **Step 6: 教师个人中心 profile.ejs + 路由（FR-03/FR-04）**

`views/teacher/profile.ejs`：结构与学生版相同（侧边栏 active: 'profile'），账号信息卡（用户名、角色"教师"）+ 修改密码表单（action="/teacher/profile"，字段 current/password/confirm）+ 退出登录按钮（POST /logout 内嵌表单）。

`routes/teacher.js` 追加：

```js
const auth = require('../auth');

// 个人中心
router.get('/profile', (req, res) => res.render('teacher/profile'));
router.post('/profile', auth.changePassword);
```

- [ ] **Step 7: 自测**

用教师账号 `tea_test`（Task 3 注册）登录后浏览器验证：`/teacher` 显示三张统计卡与侧边菜单；`/teacher/activities` 空态 + 新增按钮；`/teacher/activities/new` 表单五字段齐全；侧边菜单四项可点、当前页高亮；退出登录按钮可用（POST /logout → 回登录页）；个人中心修改密码成功后旧密码登录失败、新密码可登录。学生账号访问 `/teacher` 应 403（验证点 6 提前确认，Task 9 再正式记录）。

- [ ] **Step 8: 提交**

```bash
git add .
git commit -m "步骤5: 教师端后台框架、侧边菜单与活动表单页面"
git push origin main
```

---

### Task 6: 教师发布、修改、删除活动业务

**Files:**
- Modify: `routes/teacher.js`（表单校验、create/edit/delete、真实统计、真实列表）

**Interfaces:**
- Consumes: `formatLocalTime`（统计 SQL 比较用）、`setFlash`、`getActivityStatus`
- Produces: 活动 CRUD 完成后，学生端 Task 4 列表自动显示教师新活动（同一张表，无需改动）

- [ ] **Step 1: 表单校验与越权加载工具（routes/teacher.js 顶部追加）**

```js
const { getDb } = require('../db');
const { setFlash } = require('../middleware');
const { getActivityStatus, formatLocalTime } = require('../utils/activity-status');

const TIME_RE = /^\d{4}-\d{2}-\d{2} \d{2}:\d{2}$/;

// 校验活动表单，返回 { data, errors }；errors 为空数组表示通过
function parseActivityForm(body) {
  const data = {
    title: String(body.title || '').trim(),
    time: String(body.time || '').trim(),
    location: String(body.location || '').trim(),
    description: String(body.description || '').trim(),
    max_participants: Number(body.max_participants),
  };
  const errors = [];
  if (!data.title) errors.push('请填写活动标题');
  if (!TIME_RE.test(data.time) || isNaN(new Date(data.time).getTime())) errors.push('活动时间格式应为 YYYY-MM-DD HH:MM');
  if (!data.location) errors.push('请填写活动地点');
  if (!data.description) errors.push('请填写活动简介');
  if (!Number.isInteger(data.max_participants) || data.max_participants < 1) errors.push('最大报名人数应为正整数');
  return { data, errors };
}

// 加载"本人"活动；不存在渲染 404，非本人渲染 403，均返回 null
function loadOwnActivity(req, res) {
  const activity = getDb().prepare('SELECT * FROM activities WHERE id = ?').get(Number(req.params.id));
  if (!activity) {
    res.status(404).render('404');
    return null;
  }
  if (activity.teacher_id !== req.session.user.id) {
    res.status(403).render('403', { message: '只能管理自己发布的活动' });
    return null;
  }
  return activity;
}
```

- [ ] **Step 2: 新增活动 POST /teacher/activities/new**

```js
// FR-09 创建新活动
router.post('/activities/new', (req, res) => {
  const { data, errors } = parseActivityForm(req.body);
  if (errors.length) {
    setFlash(req, 'error', errors.join('；'));
    return res.render('teacher/activity-form', { mode: 'new', activity: data });
  }
  getDb().prepare('INSERT INTO activities (title, time, location, description, max_participants, teacher_id) VALUES (?, ?, ?, ?, ?, ?)')
    .run(data.title, data.time, data.location, data.description, data.max_participants, req.session.user.id);
  setFlash(req, 'success', '活动创建成功');
  res.redirect('/teacher/activities');
});
```

- [ ] **Step 3: 编辑活动 GET + POST /teacher/activities/:id/edit**

```js
// FR-10 编辑（仅本人活动）
router.get('/activities/:id/edit', (req, res) => {
  const activity = loadOwnActivity(req, res);
  if (!activity) return;
  res.render('teacher/activity-form', { mode: 'edit', activity });
});

router.post('/activities/:id/edit', (req, res) => {
  const activity = loadOwnActivity(req, res);
  if (!activity) return;
  const { data, errors } = parseActivityForm(req.body);
  if (errors.length) {
    setFlash(req, 'error', errors.join('；'));
    return res.render('teacher/activity-form', { mode: 'edit', activity: { ...data, id: activity.id } });
  }
  getDb().prepare('UPDATE activities SET title = ?, time = ?, location = ?, description = ?, max_participants = ? WHERE id = ?')
    .run(data.title, data.time, data.location, data.description, data.max_participants, activity.id);
  setFlash(req, 'success', '活动修改成功');
  res.redirect('/teacher/activities');
});
```

- [ ] **Step 4: 删除活动 POST /teacher/activities/:id/delete**

```js
// FR-11 删除（仅本人活动；先删报名记录再删活动，避免残留数据）
router.post('/activities/:id/delete', (req, res) => {
  const activity = loadOwnActivity(req, res);
  if (!activity) return;
  getDb().prepare('DELETE FROM registrations WHERE activity_id = ?').run(activity.id);
  getDb().prepare('DELETE FROM activities WHERE id = ?').run(activity.id);
  setFlash(req, 'success', '活动已删除');
  res.redirect('/teacher/activities');
});
```

前端确认：`views/teacher/activities.ejs` 的删除按钮改为内嵌表单 + `public/js/main.js`：

```js
// 删除活动前二次确认
document.querySelectorAll('form.js-confirm').forEach((form) => {
  form.addEventListener('submit', (e) => {
    if (!window.confirm('确定要删除该活动吗？删除后学生端将不可见。')) e.preventDefault();
  });
});
```

- [ ] **Step 5: 真实统计与活动列表（替换 Task 5 的占位实现）**

```js
// 后台首页统计：总活动数、待开始、已结束（仅本人活动；字符串比较，格式固定可字典序）
router.get('/', (req, res) => {
  const db = getDb();
  const now = formatLocalTime(new Date());
  const total = db.prepare('SELECT COUNT(*) AS c FROM activities WHERE teacher_id = ?').get(req.session.user.id).c;
  const upcoming = db.prepare('SELECT COUNT(*) AS c FROM activities WHERE teacher_id = ? AND time > ?')
    .get(req.session.user.id, now).c;
  res.render('teacher/dashboard', { total, upcoming, ended: total - upcoming });
});

// 活动管理列表（仅本人活动）
// 报名表 Task 7 才建，计数先置 0（Task 7 换回真实统计）
router.get('/activities', (req, res) => {
  const rows = getDb().prepare(`
    SELECT a.*, 0 AS registered_count
    FROM activities a
    WHERE a.teacher_id = ?
    ORDER BY a.time DESC`).all(req.session.user.id);
  for (const a of rows) {
    a.status = getActivityStatus(a.time, a.registered_count, a.max_participants);
  }
  res.render('teacher/activities', { activities: rows });
});
```

- [ ] **Step 6: activities.ejs 表格渲染（替换空态逻辑）**

列：标题（链接到编辑页或名单页）、时间、状态 badge、操作（编辑 / 删除表单 / 查看报名名单按钮——名单按钮 Task 8 实现，先放禁用按钮）。每行操作：

```ejs
<a class="btn btn-secondary btn-sm" href="/teacher/activities/<%= a.id %>/edit">编辑</a>
<form class="inline-form js-confirm" action="/teacher/activities/<%= a.id %>/delete" method="POST">
  <button class="btn btn-danger btn-sm" type="submit">删除</button>
</form>
<button class="btn btn-secondary btn-sm" disabled title="下一迭代开放">报名名单</button>
```

`views/teacher/dashboard.ejs` 去掉"统计将在后续生效"说明。

- [ ] **Step 7: 自测**

1. 教师 `tea_test` 登录：新增活动（含一个过去时间"2026-09-19 10:00"的活动）→ 列表出现、状态正确（已结束橙/未开始绿）；
2. 表单校验：空标题提交 → 提示"请填写活动标题"；时间填 "10月1日" → 提示格式错误；最大人数填 0 或小数 → 提示正整数；
3. 编辑活动改标题 → 学生端 `/student` 同步显示新标题；
4. 删除活动 → 学生端不再显示；删除有 confirm 弹窗；
5. 越权：注册第二个教师 `tea_test2`，直接访问 `http://localhost:3000/teacher/activities/<tea_test的活动id>/edit` → 403"只能管理自己发布的活动"；
6. 学生 `stu_test` 访问 `/teacher` 与 `/teacher/activities` → 403。
   每个操作后页面出现对应 flash 提示（创建成功/修改成功/已删除）。

- [ ] **Step 8: 提交**

```bash
git add .
git commit -m "步骤6: 教师发布、编辑、删除活动，后台统计与越权防护"
git push origin main
```

---

### Task 7: 学生报名活动、我的报名记录

**Files:**
- Create: `views/student/my.ejs`
- Modify: `db.js`（registrations 表）、`routes/student.js`（报名 POST、我的报名 GET、列表/详情 SQL 换回真实统计）、`routes/teacher.js`（列表 SQL 换回真实统计）、`views/student/detail.ejs`（按钮状态）

**Interfaces:**
- Consumes: `getActivityStatus`、`setFlash`
- Produces: 报名记录写入 registrations；Task 8 名单读取同一张表

- [ ] **Step 1: db.js 增加 registrations 表**

```js
// 追加建表（UNIQUE 约束从数据库层面防重复报名）
db.exec(`
  CREATE TABLE IF NOT EXISTS registrations (
    id          INTEGER PRIMARY KEY AUTOINCREMENT,
    activity_id INTEGER NOT NULL REFERENCES activities(id),
    student_id  INTEGER NOT NULL REFERENCES users(id),
    created_at  TEXT NOT NULL DEFAULT (datetime('now','localtime')),
    UNIQUE (activity_id, student_id)
  );
`);
```

- [ ] **Step 2: 学生/教师路由换回真实统计 SQL**

1. `routes/student.js`：Task 4 中写死的 `registered_count = 0` / `registered_by_me = 0` 恢复为 Task 4 接口定义里的子查询版本（列表页无 `registered_by_me`，详情页两者都有）；
2. `routes/teacher.js` 的 `/activities` 列表：Task 6 的 `0 AS registered_count` 换回子查询：

```js
SELECT a.*, (SELECT COUNT(*) FROM registrations r WHERE r.activity_id = a.id) AS registered_count
```

- [ ] **Step 3: 报名 POST /student/activities/:id/register**

```js
// FR-07 报名：已结束/已满员/重复报名均拒绝并提示
router.post('/activities/:id/register', (req, res) => {
  const db = getDb();
  const activity = db.prepare('SELECT * FROM activities WHERE id = ?').get(Number(req.params.id));
  if (!activity) return res.status(404).render('404');
  const backUrl = `/student/activities/${activity.id}`;

  if (new Date(activity.time) <= new Date()) {
    setFlash(req, 'error', '该活动已结束，无法报名');
    return res.redirect(backUrl);
  }
  const count = db.prepare('SELECT COUNT(*) AS c FROM registrations WHERE activity_id = ?').get(activity.id).c;
  if (count >= activity.max_participants) {
    setFlash(req, 'error', '该活动报名人数已满');
    return res.redirect(backUrl);
  }
  try {
    db.prepare('INSERT INTO registrations (activity_id, student_id) VALUES (?, ?)')
      .run(activity.id, req.session.user.id);
    setFlash(req, 'success', '报名成功');
  } catch (err) {
    // 唯一约束兜底：并发/绕过前端时仍拒绝重复报名
    if (String(err.message).includes('UNIQUE')) {
      setFlash(req, 'error', '您已报名该活动，不可重复报名');
    } else {
      throw err;
    }
  }
  res.redirect(backUrl);
});
```

- [ ] **Step 4: detail.ejs 按钮状态（FR-07 展示）**

```ejs
<% if (activity.registered_by_me) { %>
  <button class="btn btn-secondary" disabled>已报名，不可重复报名</button>
<% } else if (activity.status.key === 'ended') { %>
  <button class="btn btn-secondary" disabled>活动已结束</button>
<% } else if (activity.status.key === 'full') { %>
  <button class="btn btn-secondary" disabled>报名人数已满</button>
<% } else { %>
  <form action="/student/activities/<%= activity.id %>/register" method="POST">
    <button class="btn btn-primary" type="submit">立即报名</button>
  </form>
<% } %>
```

- [ ] **Step 5: 我的报名 GET /student/my + my.ejs（FR-08）**

```js
// FR-08 我的报名记录（含活动状态）
router.get('/my', (req, res) => {
  const rows = getDb().prepare(`
    SELECT a.*, r.created_at AS registered_at, u.username AS teacher_name,
      (SELECT COUNT(*) FROM registrations r2 WHERE r2.activity_id = a.id) AS registered_count
    FROM registrations r
    JOIN activities a ON a.id = r.activity_id
    JOIN users u ON u.id = a.teacher_id
    WHERE r.student_id = ?
    ORDER BY r.created_at DESC`).all(req.session.user.id);
  for (const a of rows) {
    a.status = getActivityStatus(a.time, a.registered_count, a.max_participants);
  }
  res.render('student/my', { rows });
});
```

`views/student/my.ejs`（顶部导航 active: 'my'）：表格 `.table`：活动名称（链接到详情）/ 活动时间 / 报名时间 / 状态 badge / 操作"查看详情"。空态"还没有报名任何活动，去首页看看吧"。

- [ ] **Step 6: 自测**

（服务重启后会话失效，先重新登录：`curl -s -c /tmp/jar.txt -X POST http://localhost:3000/login -d "username=stu_test&password=123456" -o /dev/null`，教师同理）

1. 学生 `stu_test` 登录：详情页点"立即报名" → flash"报名成功"，按钮变灰"已报名，不可重复报名"；
2. 直接 POST 重复报名：`curl -s -b /tmp/jar.txt -L -X POST http://localhost:3000/student/activities/<id>/register | grep "不可重复报名"`；
3. 满员：教师建 `max_participants=1` 的活动 → stu_test 报名成功后，注册学生 `stu_test2` 登录报名 → 提示"报名人数已满"；
4. 已结束：对种子活动 `2026-09-10 读书分享会`（id 查询确认）POST 报名 → 提示"该活动已结束，无法报名"；
5. `/student/my` 显示已报名记录、状态 badge 正确；
6. 教师登录后 POST `/student/activities/<id>/register` → 403（教师不能报名）。

- [ ] **Step 7: 提交**

```bash
git add .
git commit -m "步骤7: 学生报名活动与我的报名记录，防重复防满员校验"
git push origin main
```

---

### Task 8: 教师查看报名名单

**Files:**
- Create: `views/teacher/roster.ejs`
- Modify: `routes/teacher.js`（roster GET）、`views/teacher/activities.ejs`（名单按钮启用）

**Interfaces:**
- Consumes: `loadOwnActivity`（Task 6）
- Produces: 名单页数据 `{ activity, students }`，students 每项 `{ username, created_at }`

- [ ] **Step 1: roster 路由（FR-12）**

```js
// FR-12 查看报名名单（仅本人活动，仅展示用户名，V1.0 不导出）
router.get('/activities/:id/roster', (req, res) => {
  const activity = loadOwnActivity(req, res);
  if (!activity) return;
  const students = getDb().prepare(`
    SELECT u.username, r.created_at
    FROM registrations r
    JOIN users u ON u.id = r.student_id
    WHERE r.activity_id = ?
    ORDER BY r.created_at ASC`).all(activity.id);
  res.render('teacher/roster', { activity, students });
});
```

- [ ] **Step 2: roster.ejs**

内容：返回活动管理链接；活动信息卡（标题、时间、地点、报名人数 `students.length`/最大人数）；报名学生表格（序号/用户名/报名时间）；空态"暂无学生报名"。仅查看，无任何导出按钮。

- [ ] **Step 3: activities.ejs 名单按钮启用**

Task 6 的禁用按钮替换为：`<a class="btn btn-secondary btn-sm" href="/teacher/activities/<%= a.id %>/roster">报名名单</a>`

- [ ] **Step 4: 自测**

（服务重启后会话失效，先重新登录教师账号 tea_test）

1. 教师 `tea_test` 查看有报名活动的名单 → 显示 stu_test、stu_test2 用户名与报名时间；
2. 无报名活动名单 → 空态；
3. `tea_test2` 访问 `tea_test` 活动的 `/roster` → 403；
4. 学生访问 `/teacher/activities/<id>/roster` → 403。

- [ ] **Step 5: 提交**

```bash
git add .
git commit -m "步骤8: 教师查看活动报名学生名单"
git push origin main
```

---

### Task 9: 整体 bug 修复与验证测试

**Files:**
- Create: `test/smoke.js`
- Modify: `README.md`（运行说明、演示账号、验证记录）、各处修复的 bug

**Interfaces:**
- Consumes: 全部既有路由与提示文案

- [ ] **Step 1: test/smoke.js（冒烟验证脚本）**

```js
// 冒烟验证：对运行中的服务依次验证核心业务链路，任一步失败即报错退出
// 运行前提：npm start 已启动；运行方式：npm run smoke
const BASE = process.env.BASE_URL || 'http://localhost:3000';

// 带 cookie 的简易 HTTP 客户端（Node 24 内置 fetch）
function makeClient() {
  let cookie = '';
  return {
    async request(method, path, body) {
      const res = await fetch(BASE + path, {
        method,
        headers: { cookie, ...(body ? { 'content-type': 'application/x-www-form-urlencoded' } : {}) },
        body: body ? new URLSearchParams(body).toString() : undefined,
        redirect: 'manual',
      });
      const setCookie = res.headers.get('set-cookie');
      if (setCookie) cookie = setCookie.split(';')[0];
      return res;
    },
  };
}

function check(ok, message) {
  if (!ok) {
    console.error(`✘ 失败: ${message}`);
    process.exit(1);
  }
  console.log(`✔ ${message}`);
}

async function main() {
  const suffix = Date.now(); // 用户名加时间戳，支持重复运行
  const teacher = makeClient();
  const student = makeClient();
  const student2 = makeClient();
  const futureTime = '2027-01-01 10:00';

  // 1. 注册教师与学生（FR-01）
  let res = await teacher.request('POST', '/register', {
    username: `tea_${suffix}`, password: 'pass123', confirm: 'pass123', role: 'teacher',
  });
  check(res.status === 302, '教师注册成功');
  res = await student.request('POST', '/register', {
    username: `stu_${suffix}`, password: 'pass123', confirm: 'pass123', role: 'student',
  });
  check(res.status === 302, '学生注册成功');

  // 2. 登录按角色跳转（FR-02）
  res = await teacher.request('POST', '/login', { username: `tea_${suffix}`, password: 'pass123' });
  check(res.status === 302 && res.headers.get('location') === '/teacher', '教师登录跳转 /teacher');
  res = await student.request('POST', '/login', { username: `stu_${suffix}`, password: 'pass123' });
  check(res.status === 302 && res.headers.get('location') === '/student', '学生登录跳转 /student');

  // 3. 教师创建活动（FR-09）
  res = await teacher.request('POST', '/teacher/activities/new', {
    title: `冒烟测试活动_${suffix}`, time: futureTime, location: '测试场地',
    description: '冒烟测试用活动', max_participants: '1',
  });
  check(res.status === 302, '教师创建活动成功');

  // 4. 学生端可见该活动（FR-05）
  res = await student.request('GET', '/student');
  const listText = await res.text();
  check(listText.includes(`冒烟测试活动_${suffix}`), '学生活动列表可见新活动');

  // 5. 学生报名成功；重复报名被拒（FR-07）
  // 从列表页提取新建活动的 id（标题卡片内的详情链接）
  const match = listText.match(new RegExp(`冒烟测试活动_${suffix}[\\s\\S]*?href="/student/activities/(\\d+)"`));
  check(match !== null, '能从列表页提取活动详情链接');
  const activityId = Number(match[1]);
  res = await student.request('POST', `/student/activities/${activityId}/register`, {});
  check(res.status === 302, '学生报名请求被接受');
  res = await student.request('GET', `/student/activities/${activityId}`);
  check((await res.text()).includes('已报名'), '详情页显示已报名');
  res = await student.request('POST', `/student/activities/${activityId}/register`, {});
  res = await student.request('GET', `/student/activities/${activityId}`);
  check((await res.text()).includes('不可重复报名'), '重复报名被拒绝');

  // 6. 满员拒绝（FR-07）：max_participants=1，第二个学生报名被拒
  res = await student2.request('POST', '/register', {
    username: `stu2_${suffix}`, password: 'pass123', confirm: 'pass123', role: 'student',
  });
  res = await student2.request('POST', '/login', { username: `stu2_${suffix}`, password: 'pass123' });
  res = await student2.request('POST', `/student/activities/${activityId}/register`, {});
  res = await student2.request('GET', `/student/activities/${activityId}`);
  check((await res.text()).includes('报名人数已满'), '满员报名被拒绝');

  // 7. 教师名单包含报名学生（FR-12）
  res = await teacher.request('GET', `/teacher/activities/${activityId}/roster`);
  check((await res.text()).includes(`stu_${suffix}`), '教师名单包含报名学生');

  // 8. 权限隔离（NFR-01）：学生访问教师后台 → 403
  res = await student.request('GET', '/teacher');
  check(res.status === 403, '学生访问教师后台返回 403');
  res = await student.request('GET', `/teacher/activities/${activityId}/roster`);
  check(res.status === 403, '学生访问名单页返回 403');

  // 9. 教师删除活动后学生端不可见（FR-11）
  res = await teacher.request('POST', `/teacher/activities/${activityId}/delete`, {});
  check(res.status === 302, '教师删除活动成功');
  res = await student.request('GET', '/student');
  check(!(await res.text()).includes(`冒烟测试活动_${suffix}`), '删除后学生端不再显示该活动');

  console.log('\n全部验证通过 ✔ 核心业务流程跑通');
}

main().catch((err) => {
  console.error(`✘ 异常: ${err.message}`);
  process.exit(1);
});
```

注意：第 5 步提取 activityId——用正则 `冒烟测试活动_${suffix}` 在列表文本中定位其卡片内 `href="/student/activities/(\d+)"`。实现时按此思路写具体正则，测试前先人工验证该正则能命中。

- [ ] **Step 2: 全流程回归（docs/04 附录 7 条验证点）**

按顺序手动执行并记录结果到 `devlog/2026-09-20.md` 的验证记录小节（或新建验证记录文件，链接进 README）：
1. 注册学生/教师账号、登录跳转对应页面 —— 通过/失败 + 截图或说明
2. 教师新建活动，学生前台可见
3. 学生报名，不能重复报名
4. 教师查看名单含该学生
5. 教师编辑/删除活动，学生端同步变化
6. 学生访问教师后台 URL → 403
7. V1.0 目标判定：核心业务全部可运行

- [ ] **Step 3: 修复回归中发现的 bug**

每发现一个 bug：先在 devlog 记录现象 → 定位（先读相关文件）→ 修复 → 复测该验证点通过。

- [ ] **Step 4: README 更新**

补充：演示账号（teacher_demo / demo123456，种子数据说明）；`npm run smoke` 用法；验证记录链接；Git 提交历史说明（`git log` 展示迭代过程）。

- [ ] **Step 5: 冒烟脚本运行**

```bash
npm start &
npm run smoke    # 预期输出全部 ✔，最后"全部验证通过"
```

失败则按报错信息修复后重跑，直到全绿。

- [ ] **Step 6: 提交**

```bash
git add .
git commit -m "步骤9: 整体修复与冒烟验证，README完善"
git push origin main
```

---

## 完成判定

9 个任务全部 commit 且冒烟脚本全绿、7 条手动验证点通过 = V1.0 达成（docs/01 第 2.5 节完成判定标准）。push 若受网络限制，待网络恢复后执行 `git push origin main` 补推全部提交。
