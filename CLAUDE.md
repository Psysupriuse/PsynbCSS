# Psy剪贴板 — 项目工作指引

Windows 剪贴板历史工具:自动记录复制内容(文字+图片),托盘常驻,支持搜索、置顶、一键回贴、按保存期限自动清理。UI 淡蓝色、简洁直观。

## 📁 标准文件索引(开发前先读,以它们为准)

| 文件 | 内容 | 何时读 |
|---|---|---|
| [docs/01-产品需求.md](docs/01-产品需求.md) | 功能需求、已确认决策记录 | 需求不明时 |
| [docs/02-技术方案.md](docs/02-技术方案.md) | 架构、数据设计、关键机制 | 改动主进程前 |
| [docs/03-设计规范.md](docs/03-设计规范.md) | 配色、布局、卡片与交互规范 | 改动 UI 前 |
| [docs/04-开发步骤与验收标准.md](docs/04-开发步骤与验收标准.md) | 阶段划分、每阶段验收标准 | 每次开工前 |
| [docs/05-测试清单.md](docs/05-测试清单.md) | 全功能总验收清单 | 全部阶段完成后 |
| [devlogs/](devlogs/) | 每日开发日志(按日期命名) | 每次开工/收工 |

## 📝 工作约定(必须遵守)

1. **分阶段小步推进**:严格按 docs/04 的阶段顺序执行,上一阶段验收通过后才进入下一阶段;一个阶段只做一件事,不做范围外改动。
2. **开发日志**:每次开发会话开始前,创建/读取当天日志 `devlogs/YYYY-MM-DD.md`(格式:今日完成 / 待办 / 备注);会话收工时更新"今日完成"与"待办"。这是自动工作流的一部分,无需用户提醒。
3. **先文档后代码**:需求变更、关键机制改动 → 先更新 docs/ 对应文档,再改代码。
4. **每步必验证**:改动后立即运行验证(命令见下),验证不通过不进入下一步,不在未验证的代码上继续堆叠。
5. **安全底线**:数据只存本机、无网络请求;contextIsolation 保持开启、nodeIntegration 保持关闭。

## 🔧 常用命令

| 命令 | 用途 |
|---|---|
| `npm start` | 启动应用(开发模式) |
| `npm test` | 数据层自检:自动跑断言(记录/去重/过滤/持久化/容量/到期清理)后退出 |
| `npm run uitest` | UI 交互自检:模拟真实按键/点击跑端到端断言(搜索/置顶/删除/回贴/设置)后退出 |
| `npm run shot` | 截图验证:注入示例数据、截图 shot.png + 报告 shot-report.json 后退出(数据写入 .devdata/,不影响真实数据) |
| `node scripts/make-icon.js` | 重新生成图标 |
| `npm run dist` | 打包 NSIS 安装包(首次需下载打包工具,可用国内镜像) |

> 注意:测试会写入系统剪贴板,运行测试前先退出正在运行的 Psy剪贴板 实例,避免测试内容进入真实历史。

## 🏗 架构速查

- **主进程** `main.js`:托盘 / 全局快捷键 / 剪贴板轮询(700ms) / 存储 / 清理(每小时) / 自动粘贴(PowerShell SendKeys)
- **渲染进程** `renderer/`:原生 JS,通过 `preload.js` 暴露的 `window.psy` API 与主进程通信
- **数据**:`%APPDATA%` 下 userData 目录(history.json + images/ + settings.json);截图模式使用 `.devdata/` 隔离
- **打包**:electron-builder → NSIS 安装包,产物在 `dist/`;文件白名单见 package.json `build.files`

## 📌 当前状态

见 devlogs/ 最新日志的"待办"部分。
